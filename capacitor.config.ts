import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'wedesign.android',
  appName: 'wedesign',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
